{
  "name": "turbo-example",
  "private": true,
  "packageManager": "bun@1.3.11",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "format": "turbo run format",
    "check": "turbo run check",
    "e2e": "turbo run e2e",
    "dev": "turbo run dev",
    "platform:info": "turbo run platform:info"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.4",
    "@playwright/test": "^1.60.0",
    "@repo/biome-config": "workspace:*",
    "playwright": "^1.60.0",
    "turbo": "^2.5.5",
    "typescript": "^5.9.2"
  }
}
